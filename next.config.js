/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [], // если будете использовать внешние изображения
  },
}

module.exports = nextConfig
